// /review/[type]/[id] — standalone review view (completion · deployment · class · exercise). U5.
import React from 'react';
import { RecordReviewRoute } from '@/features/review/RecordReviewScreen';

export default function Route() {
  return <RecordReviewRoute />;
}
