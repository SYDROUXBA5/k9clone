// /reports — the "Create Printable Report" dialog (U6). /reports/view renders the result.
import React from 'react';
import { ReportDialogScreen } from '@/features/reports/ReportDialogScreen';

export default function Route() {
  return <ReportDialogScreen />;
}
