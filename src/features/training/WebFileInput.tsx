// Native placeholder — the web build resolves WebFileInput.web.tsx instead.
import type React from 'react';

export function WebFileInput(_props: { inputRef: React.MutableRefObject<{ click: () => void } | null>; onFiles: (f: FileList) => void; testID: string }): React.ReactElement | null {
  return null;
}
